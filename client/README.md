# Windows Client Bootstrap

This client is a bootstrap tool to validate the server-side live ingest path.

It captures webcam frames locally and sends them to the server orchestrator.

This is not the final low-latency media transport. The product path will move to a GStreamer/WebRTC-based media pipeline.